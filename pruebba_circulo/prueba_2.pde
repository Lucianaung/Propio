/*
PImage img;
 float offset = 0;
 float easing = 0.05;
 
 void setup(){
 size(640, 400);
 img = loadImage("tex2.jpg");  // Load an image into the program
 
 }
 
 void draw(){
 fill(#F2E7BB);
 rect(0,0,320,400);
 fill(0);
 rect(320,0,320,400);
 
 tint(255, 100);  // Display at half opacity
 image(img, offset, 0);
 }

 */
 
 //CAPA DE TEXTURA
 /*
 textura.beginDraw();
  tint(255, 50);
  image(textura, 80,80);
  textura.endDraw();
  image(textura, 0, 0);
 */
